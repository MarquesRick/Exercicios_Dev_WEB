const [a, b, c, d] = [3, 5, 1, 15] //Usando o Destructuring para criar varias constantes de uma só vez

const soma = a + b + c + d
const subtracao = d - b
const mult = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, mult, -divisao, modulo) //colocando o sinal de negativo, o resultado será negativo
